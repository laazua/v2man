"""Django app config for the traffic module."""

from django.apps import AppConfig


class TrafficConfig(AppConfig):
    """Django app configuration for the traffic application."""

    name = "traffic"
